// frontend/app/blog/page.tsx
import BlogIndexClient from "./BlogIndexClient";

export const metadata = { title: "Blog — PerchRole" };

export default function BlogIndexPage() {
  return <BlogIndexClient />;
}