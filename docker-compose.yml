# docker-compose.yml — local dev only
services:
  backend:
    build: ./backend
    ports: ["8080:8080"]
    env_file: ./backend/.env
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
    env_file: ./frontend/.env.local